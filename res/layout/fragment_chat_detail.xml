<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_application"
    android:orientation="vertical" >

    <com.application.ui.view.ChatListView
        android:id="@+id/fragmentChatListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/chat_compose_panel"
        android:layout_marginBottom="-3dp"
        android:cacheColorHint="#00000000"
        android:clipToPadding="false"
        android:divider="@null"
        android:dividerHeight="7dp"
        android:fadingEdge="none"
        android:fadingEdgeLength="0dp"
        android:listSelector="@android:color/transparent"
        android:paddingBottom="3dp"
        android:paddingTop="4dp"
        android:stackFromBottom="true" />

    <RelativeLayout
        android:id="@id/chat_compose_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/chattext_background"
        android:minHeight="48dp" >

        <ImageView
            android:id="@+id/fragmentChatSendIv"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentRight="true"
            android:layout_marginRight="8dp"
            android:background="@color/chattext_background"
            android:padding="4dp"
            android:src="@drawable/ic_chat_send"
            tools:ignore="RtlHardcoded,ContentDescription" />

        <android.support.v7.widget.AppCompatEditText
            android:id="@+id/fragmentChatMessageEv"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignRight="@id/fragmentChatSendIv"
            android:layout_marginLeft="8dp"
            android:layout_marginRight="60dp"
            android:background="@color/chattext_background"
            android:hint="Send Message..."
            android:maxHeight="150dp"
            android:minHeight="48dp"
            android:textSize="16sp"
            tools:ignore="RtlHardcoded,HardcodedText" />
    </RelativeLayout>

</RelativeLayout>